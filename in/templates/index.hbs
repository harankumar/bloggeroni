<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{name}}</title>
    <link rel="stylesheet" href="./assets/index.css">
</head>
<body>

<div id="top">
    <h1>{{name}}</h1>

    <i>{{{description}}}</i>
</div>

<div id="posts">
    {{#posts}}
        {{#unless @first}}
            <hr>
        {{/unless}}
        <a class="post" href="{{id}}.html">
            <h2>{{title}}</h2>
            <h3>{{subtitle}}</h3>
            <i>{{date}}</i>
        </a>
    {{/posts}}
</div>

</body>
</html>